name: Deploy Frontend
run-name: Deploy Frontend by ${{ github.actor }}

on:
  push:
    branches:
      - main
    paths:
      - src/static/**

# jobs:
#   fronted_cicd:
#     name: Deploy Frontend
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check Repo
#         uses: actions/checkout@v4

#       - name: Deploy Frontend
#         # uses: ./.github/actions/deploy_frontend
#         run: |
#           echo "Deploying frontend..."
#           aws --version

#           # aws s3 sync src/static/ s3://json-validator-frontend/static/ --exclude "index.html"
#           # aws s3 cp src/static/index.html s3://json-validator-frontend/

#           # aws cloudfront create-invalidation --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"

#         env:
#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           AWS_DEFAULT_REGION: us-east-1